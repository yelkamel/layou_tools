name: layou_tools
description: some dev tools to build amazing apps.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase
  cloud_firestore: ^5.4.2
  cloud_firestore_web: ^4.3.1
  firebase_auth: ^5.3.0
  firebase_auth_web: ^5.13.1

  # Animation
  lottie: ^1.2.0

  # Utils
  intl: ^0.18.1
  json_serializable: ^6.0.1
  json_annotation: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.7