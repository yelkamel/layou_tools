name: layou_tools
description: some dev tools to build amazing apps.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  lottie:
  intl:
  json_serializable:
  json_annotation:
  uuid:

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.7