name: layou_tools
description: some dev tools to build amazing apps.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  lottie: ^1.2.0 
  intl: ^0.19.0
  json_serializable: ^6.0.1
  json_annotation: ^4.4.0
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.7